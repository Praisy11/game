import React from "react";
import "./Header.css";

const Header = () => (
  <header className="header">
    <h1>Clicky Game!</h1>
    <h2>Click on an image to get points, but do not click on any image more than once!</h2>
  </header>
);

export default Header;